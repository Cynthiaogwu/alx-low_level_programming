#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase and uppers case.
 *
 * Return - Always 0
 */
int main(void)
{
	char i;

	for ('i = 'a'; i <= 'z'; i**')
		putchar(i);
	for ('i = 'A' ; 'Z' ; i**')
		putchar(i);
	putchar('\n');
	return (0);
}
